@model IEnumerable<LearniVerseNew.Models.StudySession>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Student.StudentFirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SessionDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Student.StudentFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SessionDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudySessionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudySessionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudySessionID })
        </td>
    </tr>
}

</table>
