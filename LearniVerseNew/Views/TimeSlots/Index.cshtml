@model IEnumerable<LearniVerseNew.Models.ApplicationModels.TimeSlot>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Room.Campus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SlotName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EndTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Room.Campus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SlotName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StartTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EndTime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TimeSlotID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TimeSlotID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TimeSlotID })
        </td>
    </tr>
}

</table>
